import './Loading.css';

import { Box } from '@mui/material';

const LoadingComponent = () => <Box>
        <div className="loader-container">
          <div className="spinner" />
      </div>
    </Box>

export default LoadingComponent;